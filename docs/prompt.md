# 题目

参考question.md文件

# 数据集

在`./data/`文件夹中有原始数据集`basic_data.xlsx`（原名为`基于强化学习的作业车间人员配置-生产调度协同优化.xlsx`），在`./data/csv_output`文件夹中，有excel文件导出的csv文件，这些csv文件把原来的excel按照不同规模和复杂度完成。

# 要求

- 对问题建模，选取合适的奖励函数，疲劳度设置合适的惩罚，完成建模后，写在`./XIWEI_code/result_XIWEI.md`文件中。

- 生成代码，放置在`./XIWEI_code`文件夹中。

- 使用pytorch完成，选取合适的规模和复杂度进行训练和测试。

- 只需生成代码，不需要运行。

- 如果没有对于文件夹或文件，请创建。

- 工作区设为本文件夹。

  